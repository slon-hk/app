import { LayoutProps } from '../types';
import { TSize } from '../helpers/size';
export interface LayoutStyleProperties {
    width: TSize;
    height: TSize;
    left: TSize;
    top: TSize;
}
export declare const transformLayoutStyleProperties: (styles?: Partial<LayoutStyleProperties>) => LayoutProps;
